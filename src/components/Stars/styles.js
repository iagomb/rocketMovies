import styled from "styled-components";

export const Container = styled.div`
        svg{
            margin-top: 10px;
            color: ${({theme})=> theme.COLORS.RED};
            font-size: 20px;
        }

`;